# compiler-explorer-builder

## New LLVM Version (or other compiler)

To add a new compiler [e.g. LLVM version], add it within data/clang-versions.txt. If the trunk LLVM version changed, also update data/trunk-llvm-version.txt

